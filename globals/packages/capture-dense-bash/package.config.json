{
  "schemaVersion": 1,
  "id": "capture-dense-bash",
  "label": "Capture Dense Bash",
  "description": "Quietly logs hard-to-allowlist shell commands to a local JSONL store. Observation only — evidence for tuning allowlists.",
  "lifecycle": "optional",
  "presentation": {
    "category": "monitoring",
    "order": 20
  },
  "resources": [
    {
      "type": "hooks",
      "source": "hooks-claude.json",
      "harness": "claude",
      "scripts": ["hooks/capture-dense-bash.mjs"]
    }
  ]
}
